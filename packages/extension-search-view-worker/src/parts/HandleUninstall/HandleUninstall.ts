import type { State } from '../State/State.ts'

// TODO show error / warning  when installment fails / times out
export const handleUninstall = async (state: State, id: any): Promise<State> => {
  return state
}
